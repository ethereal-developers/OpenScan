import 'package:flutter/material.dart';

class AppTheme {
  static Color primaryColor = Color(0xFF333333);
  static Color accentColor = Color(0xFFf37121);

  static final appTheme = ThemeData(
    primaryColor: primaryColor,
    accentColor: accentColor,
  );
}
