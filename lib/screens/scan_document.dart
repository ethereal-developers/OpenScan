import 'package:flutter/material.dart';

class ScanDocument extends StatelessWidget {

  static String route = "ScanDocument";
  
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('ScanDocument'),
    );
  }
}